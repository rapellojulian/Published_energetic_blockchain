# Copia este archivo a ".env" y completa tus valores reales.
# El archivo ".env" NUNCA debe subirse a git (ya está en .gitignore).

DATABASE_URL_RELACIONAL=postgresql://usuario:password@host/energia_relacional?sslmode=require
DATABASE_URL_SERIES=postgresql://usuario:password@host/energia_series?sslmode=require
SESSION_SECRET=julian0227
PORT=4000
