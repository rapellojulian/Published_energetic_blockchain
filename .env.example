# Copia este archivo a ".env" y completa tus valores reales.
# El archivo ".env" NUNCA debe subirse a git (ya está en .gitignore).

MONGO_URI=mongodb+srv://usuario:password@tu-cluster.mongodb.net/?appName=blockchain-energy
DB_NAME=certificacion_energetica
COLLECTION_NAME=measurements
PORT=4000
